import { HttpErrorResponseApi } from './http-error-response-api';

describe('HttpErrorResponseApi', () => {
  it('should create an instance', () => {
    expect(new HttpErrorResponseApi()).toBeTruthy();
  });
});
